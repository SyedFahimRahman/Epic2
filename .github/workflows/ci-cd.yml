name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '17'

      - name: Build and Test
        run: |
          javac *.java
          java -version
          java -cp . TaxiAppCLI
        # Additional test commands can be added here

  deploy:
    runs-on: ubuntu-latest

    needs: build

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Docker
        uses: actions/setup-docker@v2
        with:
          dockerfile: Dockerfile

      - name: Build Docker Image
        run: docker build -t taxi-app-image .

      - name: Push Docker Image to Docker Hub
        run: |
          echo ${{ secrets.dckr_pat_-cmedN00bk7vOO08t7KNXzZ1Mb8 }} | docker login -u ${{ secrets.23376066 }} --password-dckr_pat_-cmedN00bk7vOO08t7KNXzZ1Mb8
          docker tag taxi-app-image ${{ secrets.23376066 }}/taxi-app:latest
          docker push ${{ secrets.23376066 }}/taxi-app:latest
